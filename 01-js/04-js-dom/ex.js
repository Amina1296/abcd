const btn = document.getElementsByClassName('button-1')
btn.addEventListener ("click", changeColor) ;
function changeColor () {
    const disp= html;
    console.log(disp);
    
     for (let btn1 of disp)
        btn1.style.backgroundColor= 'yellow'
}